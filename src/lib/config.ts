// Bungie API keys are shipped in client-side code by design (DIM, d2foundry,
// light.gg all do the same) — the key only gates access to public game data.
// Override it in the app's Data source panel if you register your own app at
// https://www.bungie.net/en/Application.
export const DEFAULT_BUNGIE_API_KEY = "a2b3a3c648cb43a987e17a39a969cd32";
